import { useState } from 'react'; import API from '../api/api'; export default function Ats() { const [text, setText] = useState(''); const [result, setResult] = useState(null); const checkATS = async () => { const res = await API.post('/api/ats-score', { text }); setResult(res.data); }; return (<div style={{ padding: '20px' }}><h2>ATS Score Checker</h2><textarea rows='6' value={text} onChange={(e)=>setText(e.target.value)} placeholder='Paste resume text...' style={{ width: '100%' }}/><button onClick={checkATS} style={{ marginTop: '10px' }}>Check Score</button>{result && (<div><h3>Score: {result.score}/100</h3><p>Found Skills: {result.found_skills.join(', ')}</p><p>Missing Skills: {result.missing_skills.join(', ')}</p></div>)}</div>); }
